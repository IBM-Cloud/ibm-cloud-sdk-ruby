# IbmCloudIam::MFAEnrollmentStatusApi

All URIs are relative to *https://iam.cloud.ibm.com*

| Method | HTTP request | Description |
| ------ | ------------ | ----------- |
| [**create_mfa_report**](MFAEnrollmentStatusApi.md#create_mfa_report) | **POST** /v1/mfa/accounts/{account_id}/report | Trigger MFA enrollment status report for the account |
| [**get_mfa_report**](MFAEnrollmentStatusApi.md#get_mfa_report) | **GET** /v1/mfa/accounts/{account_id}/report/{reference} | Get MFA enrollment status report for the account |
| [**get_mfa_status**](MFAEnrollmentStatusApi.md#get_mfa_status) | **GET** /v1/mfa/accounts/{account_id}/status | Get MFA enrollment status for a single user in the account. |


## create_mfa_report

> <ReportReference> create_mfa_report(account_id, authorization, opts)

Trigger MFA enrollment status report for the account

Trigger MFA enrollment status report for the account by specifying the account ID. It can take a few minutes to generate the report for retrieval.

### Examples

```ruby
require 'time'
require 'ibm_cloud_iam'

api_instance = IbmCloudIam::MFAEnrollmentStatusApi.new
account_id = 'account_id_example' # String | ID of the account
authorization = 'authorization_example' # String | Authorization Token used for the request. The supported token type is a Cloud IAM Access Token. If the token is omitted the request will fail with BXNIM0308E: 'No authorization header found'. Make sure that the provided token has the required authority for the request.
opts = {
  type: 'type_example' # String | Optional report type. The supported value is 'mfa_status'. List MFA enrollment status for all the identities.
}

begin
  # Trigger MFA enrollment status report for the account
  result = api_instance.create_mfa_report(account_id, authorization, opts)
  p result
rescue IbmCloudIam::ApiError => e
  puts "Error when calling MFAEnrollmentStatusApi->create_mfa_report: #{e}"
end
```

#### Using the create_mfa_report_with_http_info variant

This returns an Array which contains the response data, status code and headers.

> <Array(<ReportReference>, Integer, Hash)> create_mfa_report_with_http_info(account_id, authorization, opts)

```ruby
begin
  # Trigger MFA enrollment status report for the account
  data, status_code, headers = api_instance.create_mfa_report_with_http_info(account_id, authorization, opts)
  p status_code # => 2xx
  p headers # => { ... }
  p data # => <ReportReference>
rescue IbmCloudIam::ApiError => e
  puts "Error when calling MFAEnrollmentStatusApi->create_mfa_report_with_http_info: #{e}"
end
```

### Parameters

| Name | Type | Description | Notes |
| ---- | ---- | ----------- | ----- |
| **account_id** | **String** | ID of the account |  |
| **authorization** | **String** | Authorization Token used for the request. The supported token type is a Cloud IAM Access Token. If the token is omitted the request will fail with BXNIM0308E: &#39;No authorization header found&#39;. Make sure that the provided token has the required authority for the request. |  |
| **type** | **String** | Optional report type. The supported value is &#39;mfa_status&#39;. List MFA enrollment status for all the identities. | [optional] |

### Return type

[**ReportReference**](ReportReference.md)

### Authorization

No authorization required

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json


## get_mfa_report

> <ReportMfaEnrollmentStatus> get_mfa_report(account_id, reference, authorization)

Get MFA enrollment status report for the account

Get MFA enrollment status report for the account by specifying the account ID and the reference that is generated by triggering the report. Reports older than a day are deleted when generating a new report.

### Examples

```ruby
require 'time'
require 'ibm_cloud_iam'

api_instance = IbmCloudIam::MFAEnrollmentStatusApi.new
account_id = 'account_id_example' # String | ID of the account
reference = 'reference_example' # String | Reference for the report to be generated, You can use 'latest' to get the latest report for the given account.
authorization = 'authorization_example' # String | Authorization Token used for the request. The supported token type is a Cloud IAM Access Token. If the token is omitted the request will fail with BXNIM0308E: 'No authorization header found'. Make sure that the provided token has the required authority for the request.

begin
  # Get MFA enrollment status report for the account
  result = api_instance.get_mfa_report(account_id, reference, authorization)
  p result
rescue IbmCloudIam::ApiError => e
  puts "Error when calling MFAEnrollmentStatusApi->get_mfa_report: #{e}"
end
```

#### Using the get_mfa_report_with_http_info variant

This returns an Array which contains the response data, status code and headers.

> <Array(<ReportMfaEnrollmentStatus>, Integer, Hash)> get_mfa_report_with_http_info(account_id, reference, authorization)

```ruby
begin
  # Get MFA enrollment status report for the account
  data, status_code, headers = api_instance.get_mfa_report_with_http_info(account_id, reference, authorization)
  p status_code # => 2xx
  p headers # => { ... }
  p data # => <ReportMfaEnrollmentStatus>
rescue IbmCloudIam::ApiError => e
  puts "Error when calling MFAEnrollmentStatusApi->get_mfa_report_with_http_info: #{e}"
end
```

### Parameters

| Name | Type | Description | Notes |
| ---- | ---- | ----------- | ----- |
| **account_id** | **String** | ID of the account |  |
| **reference** | **String** | Reference for the report to be generated, You can use &#39;latest&#39; to get the latest report for the given account. |  |
| **authorization** | **String** | Authorization Token used for the request. The supported token type is a Cloud IAM Access Token. If the token is omitted the request will fail with BXNIM0308E: &#39;No authorization header found&#39;. Make sure that the provided token has the required authority for the request. |  |

### Return type

[**ReportMfaEnrollmentStatus**](ReportMfaEnrollmentStatus.md)

### Authorization

No authorization required

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json


## get_mfa_status

> <UserMfaEnrollments> get_mfa_status(account_id, iam_id, authorization)

Get MFA enrollment status for a single user in the account.

Get MFA enrollment status for a single user in the account.

### Examples

```ruby
require 'time'
require 'ibm_cloud_iam'

api_instance = IbmCloudIam::MFAEnrollmentStatusApi.new
account_id = 'account_id_example' # String | ID of the account
iam_id = 'iam_id_example' # String | iam_id of the user. This user must be the member of the account.
authorization = 'authorization_example' # String | Authorization Token used for the request. The supported token type is a Cloud IAM Access Token. If the token is omitted the request will fail with BXNIM0308E: 'No authorization header found'. Make sure that the provided token has the required authority for the request.

begin
  # Get MFA enrollment status for a single user in the account.
  result = api_instance.get_mfa_status(account_id, iam_id, authorization)
  p result
rescue IbmCloudIam::ApiError => e
  puts "Error when calling MFAEnrollmentStatusApi->get_mfa_status: #{e}"
end
```

#### Using the get_mfa_status_with_http_info variant

This returns an Array which contains the response data, status code and headers.

> <Array(<UserMfaEnrollments>, Integer, Hash)> get_mfa_status_with_http_info(account_id, iam_id, authorization)

```ruby
begin
  # Get MFA enrollment status for a single user in the account.
  data, status_code, headers = api_instance.get_mfa_status_with_http_info(account_id, iam_id, authorization)
  p status_code # => 2xx
  p headers # => { ... }
  p data # => <UserMfaEnrollments>
rescue IbmCloudIam::ApiError => e
  puts "Error when calling MFAEnrollmentStatusApi->get_mfa_status_with_http_info: #{e}"
end
```

### Parameters

| Name | Type | Description | Notes |
| ---- | ---- | ----------- | ----- |
| **account_id** | **String** | ID of the account |  |
| **iam_id** | **String** | iam_id of the user. This user must be the member of the account. |  |
| **authorization** | **String** | Authorization Token used for the request. The supported token type is a Cloud IAM Access Token. If the token is omitted the request will fail with BXNIM0308E: &#39;No authorization header found&#39;. Make sure that the provided token has the required authority for the request. |  |

### Return type

[**UserMfaEnrollments**](UserMfaEnrollments.md)

### Authorization

No authorization required

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

